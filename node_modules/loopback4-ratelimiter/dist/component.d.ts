import { Binding, Component, ProviderMap } from '@loopback/core';
import { RateLimitMiddlewareConfig, RateLimitOptions } from './types';
export declare class RateLimiterComponent implements Component {
    private readonly ratelimitConfig?;
    private readonly configOptions?;
    constructor(ratelimitConfig?: RateLimitMiddlewareConfig | undefined, configOptions?: RateLimitOptions | undefined);
    providers?: ProviderMap;
    bindings: Binding[];
}
